Local verification: `python scripts/validate.py --index registry/index.json --sig registry/index.json.sig --pub keys/official.pub`. Lifecycle prove (Stage 1): `python scripts/lifecycle-prove.py --package owner/name --numan /path/to/numan --nu /path/to/nu`. Production signing is never done manually — it runs only inside the protected `production` GitHub Actions environment via `scripts/ci-sign.py`.